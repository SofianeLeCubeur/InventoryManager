const { escapeRegExp, requireScope } = require('./../utils');

module.exports = function(router, database, authMiddleware){

    router.post('/group', (req, res) => {

    });    
    
    router.all('/group/:id', (req, res) => {

    })    

    router.all('/group/:id/invites', (req, res) => {

    })
}