pipeline {
    agent 'any'

    stages {
        stage("build"){
            steps {
                docker.build 'im-api-server' 'WebServer/'
            }
        }

        stage("deploy"){
            steps {
                dir('WebServer/'){
                    sh './deploy.sh'
                }
            }
        }
    }
}