pipeline {
    agent any

    stages {
        stage("build"){
            steps {
                sh 'docker build -t im-api-server WebServer'
            }
        }

        stage("deploy"){
            steps {
                echo 'DEPLOYING'
                sh 'docker-compose up -d'
            }
        }
    }
}